1. Used absolute path for properties file.

2. Unsuported character encoding utf-8 for database.

3. For search not used regular expression.

4. In DAO sources not perform DRY. For example in DAOTeacher.deleteCurator method.

5. Don't work messages if write wrong data in forms

6. Hibernate n + 1 problem